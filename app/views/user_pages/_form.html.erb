<%= form_for(@user_page) do |f| %>
  <% if @user_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_page.errors.count, "error") %> prohibited this user_page from being saved:</h2>

      <ul>
      <% @user_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">

      <%= f.text_field :post ,:placeholder => 'Way to go here...',:class => 'form-control' %>
</div>

  <div class="actions">
    <%= f.submit "Post" ,:class => " btn btn-primary" %>
  </div>
<% end %>
</div>